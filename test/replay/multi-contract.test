# Test multi-contract replay functionality
# REQUIRES: stylusdb
# RUN: %cargo-stylus replay --tx %{caller_tx} --endpoint %{rpc_url} --contracts %{counter_address}:%{contracts}/counter,%{caller_address}:%{contracts}/caller --debugger stylusdb 2>&1 | FileCheck %s

# CHECK: Building Stylus contract at
# CHECK: from {{.*}}counter
# CHECK: {{(stylusdb|Compiling|Blocking waiting for file lock)}}